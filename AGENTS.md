# dsh-motion

DeepSeek Harness browser motion plugin. Keep the runtime semantic, theme-aware, accessible, and independent of host component implementation details.

## Agent skills

### Issue tracker

Issues and PRDs are stored as local Markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository: use the root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
