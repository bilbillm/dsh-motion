# Add the browser motion plugin to a Harness profile's client roster.
# The node half is intentionally empty; the browser half is served through
# the dsh.client declaration in package.json.
- insert:
    - id: dsh-motion
      name: '@dsh-external/dsh-motion'
